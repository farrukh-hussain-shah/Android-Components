package com.example.radiobutton

// TODO("Pass Data Form one Activity to Other"
/*
1st we need companion objects because we need to intialize two object else it error occur so for this we created "utils" -> "class Constants"-> companion objects
then we create a button to next activity in this we type "intent.putExtra(......,"the messes we want to tranfer")
then go to other acitivlity create a object of like "val data = inten.extras"

  then data?.let{
  in this intize the data
  and then set it to textView.text
 */